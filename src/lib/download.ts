/* ============================================================
   حفظُ ملفٍّ من المتصفّح.

   ═══ لماذا موضعٌ واحد بعد ثلاثة ═══

   كانت هذه الأسطرُ الستة منسوخةً في ثلاثة مواضع — `csv.ts` و`xlsx.ts`
   و«التحليلات» — وثلاثتُها تحمل الخطأين نفسيهما. ونسخةٌ رابعة كانت ستحمله
   كذلك. فالمنطق هنا، والمواضع الثلاثة تناديه.

   ═══ الخطأان، وكلاهما يظهر على آيفون وحده ═══

   **العنصر لم يكن يُلحق بالمستند.** و`click()` على عنصرٍ خارج الشجرة لا
   يتنقّل في WebKit — فلا يقع شيء إطلاقاً. ويعمل في كروم على سطح المكتب،
   فيمرّ الخطأ في كل تجربة إلا التي يُشتكى منها.

   **والعنوان كان يُبطَل فور النقر.** والتنزيل يبدأ بعد النقر لا معه، فقد
   يجد المتصفّحُ العنوانَ مُبطَلاً قبل أن يقرأ منه — وهذا هو ما رصده
   «التقارير» قبلنا وعلّق عليه في `saveBlob`. فالمهلةُ هنا نفسُها.

   ═══ ولا مشاركة هنا ═══

   على آيفون يُسلّم الملفّ لتطبيق ناف، وهو يعرضه في شاشةٍ فيها «مشاركة»
   و«إغلاق». فلا يُبنى في الويب زرُّ مشاركةٍ ثانٍ لما يملكه الغلاف.
   ============================================================ */

/** كم يبقى عنوانُ الكائن حيّاً بعد النقر — دقيقة تكفي أبطأ تنزيلٍ يبدأ. */
const REVOKE_AFTER_MS = 60_000;

/** يحفظ كائناً ملفّاً باسمه. */
export function saveBlob(blob: Blob, filename: string): void {
  const url = URL.createObjectURL(blob);
  const link = document.createElement('a');
  link.href = url;
  link.download = filename;
  document.body.appendChild(link);
  link.click();
  link.remove();
  window.setTimeout(() => URL.revokeObjectURL(url), REVOKE_AFTER_MS);
}

/** يحفظ نصّاً ملفّاً باسمه ونوعه. */
export function saveText(content: string, filename: string, type: string): void {
  saveBlob(new Blob([content], { type }), filename);
}
