// تطبيعُ الأرقام الهندية إلى الغربية.
//
// ═══ لماذا موضعٌ محايد ═══
//
// «٠٥٠١٢٣٤٥٦٧» و«0501234567» رقمٌ واحد كتبه رجلان. وحفظُهما كما وردا يجعل
// البحثَ عن أحدهما لا يجد الآخر، والمقارنةَ تقول إنّهما اثنان — فيصير للموكّل
// الواحد ملفّان.
//
// وهذا يقع في كل مدخل: سحبِ بيسكامب، ورفعِ ملفّ CSV، ويدٍ تكتب في الشاشة.
// فالدالّة هنا لا في مجلَّد بيسكامب، ويناديها `resources.js` على أعمدة
// الأرقام وحدها — لا على الملاحظات، فمن كتب فيها رقماً هندياً قصده.

const ARABIC_INDIC = /[٠-٩۰-۹]/g;

/** يحوّل ما في النصّ من أرقامٍ هندية (عربية ‎٠٩‎ وفارسية ‎۰۹‎) إلى ‎09‎. */
export function toLatinDigits(text) {
  return String(text ?? '').replace(ARABIC_INDIC, (digit) => {
    const code = digit.codePointAt(0);
    const base = code >= 0x06f0 ? 0x06f0 : 0x0660;
    return String(code - base);
  });
}
