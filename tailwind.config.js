/* ============================================================
   لا إعداد هنا — وهذا هو المقصود.

   كان هذا الملفّ يحمل ثيماً موازياً: عائلة خطّ (Tahoma) ولونين
   (‎naf-blue: #1e40af‎ و‎naf-gold: #f59e0b‎) مكتوبين خاماً. وهو ما
   تسمّيه القاعدة «نظاماً موازياً»: مصدرُ حقيقةٍ ثانٍ في المستودع
   نفسه، وهو أسوأ من غياب النظام أصلاً.

   الثيم كلّه صار في ‎src/registry/naf/naf-theme.css‎ يُقرأ من السجلّ
   ‎fahadaali/naf-ui‎. وTailwind v4 يقرأ إعداده من CSS لا من ملفّ
   جافاسكربت، فلا يُقرأ هذا الملفّ إطلاقاً ما لم يُشَر إليه بـ‎@config‎ —
   ولا يُشار.

   يبقى الملفّ ولا يُحذف: أدوات كثيرة تفتّش عنه، وغيابُه سؤالٌ بلا
   جواب. أمّا وجودُه فارغاً فجوابٌ مكتوب.

   لا تُعِد إليه لوناً ولا خطاً ولا مقاساً. القيمة الناقصة تُسجَّل في
   ‎naf-ui‎ ثم تُستهلك، ولا تُضاف هنا.
   ============================================================ */

export default {};
