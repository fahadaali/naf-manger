// الخروج — محليٌّ ووجهتُه المركز. والجملتان معاً لا إحداهما.
//
// الحزمة تحذف مفتاح الجلسة من `KV` ثم تمسح الكوكي. والحذف هو الخروج فعلاً:
// الكوكي الممسوح يبقى صالحاً عند من نسخ قيمته قبله، والمفتاح المحذوف لا يبقى.
//
// ثم يخرج المتصفّح إلى `‎{issuer}/` لا إلى `‎/`: جذر المنصة محميّ، فتحويلُه
// إليه يمرّ بالوسيط الذي يعيده إلى `‎/go/:id`، وجلسةُ المركز لم تُمسّ فتصدر
// رمزاً جديداً — فيعود الخارجُ إلى الشاشة التي خرج منها قبل أن يقرأ شيئاً،
// ويقرأ من ذلك أن الزرّ لا يعمل. وهو يعمل، والوجهة كانت داخل السياج.
//
// ولا تُبطَل جلسة المركز من هنا: إنهاؤها من منصة يُخرج صاحبها من المنصات
// الأخرى وهو لم يطلب ذلك.

import { pagesLogout } from 'naf-auth';

import { platformConfig } from '../_lib/config.js';

export const onRequest = (context) => pagesLogout(platformConfig(context.env))(context);
